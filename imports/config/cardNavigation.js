//Number of degress to register a left and right card swipe
let swipeThreshold = 45;

//Allow arrow key navigation to wrap around to index. First to last card and vice versa
let allowIndexWrap = false;

module.exports = {
	swipeThreshold,
	allowIndexWrap
};
