before_script:
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
  - eval $(ssh-agent -s)

staging:
  stage: test
  script:
    - ssh-add <(echo "$STAGING_SSH_PRIVATE_KEY")
    - mkdir -p ~/.ssh
    - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
    - echo $CI_BUILD_ID
    - echo $CI_BUILD_REF
    - echo $CI_BUILD_REF_NAME
    - ssh $STAGING_USER@$STAGING_HOST 'cd flashcards/; git fetch --all; commitid='git rev-parse --short HEAD'; git reset --hard $commitid'
  tags:
    - nodejs

production:
  stage: deploy
  script:
    - ssh-add <(echo "$PRODUCTION_SSH_PRIVATE_KEY")
    - mkdir -p ~/.ssh
    - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
    - ssh $PRODUCTION_USER@$PRODUCTION_HOST 'cd flashcards/; git fetch --all; git reset --hard origin/master'
  only:
    - master
  tags:
    - nodejs
